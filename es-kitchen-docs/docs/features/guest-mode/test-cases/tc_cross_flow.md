# TC: Cross-Flow — Guest Upgrade, Data Persistence, Edge Cases

> **Module:** Cross-flow — các luồng kết hợp nhiều US, edge cases, negative cases đặc thù
> **Scope:** Mobile App E01 + Admin Web E03 + API (indirect)
> **Related US:** US-01, US-02, US-03, US-04, US-05, US-06
> **Related BR:** BR-01 đến BR-13 (tổng hợp)
> **Related AC:** AC-01-5, AC-01-6, AC-03-5, AC-04-8, AC-05-3, AC-05-4

---

## Bảng Test Cases

| ID | Function Name | Category | Risk Level | Test Scenario | Precondition | Steps | Expected Results | Test Data | Priority |
|---|---|---|---|---|---|---|---|---|---|
| GM_CROSS_TC_001 | Cross Flow | Full Upgrade Flow | High | Check full flow: Guest login → duyệt menu → checkout → upgrade → xem order history | Môi trường staging, test company `COMP-CROSS-001` có guestPaymentAllowed=true, elepay sandbox | 1. Mở app, bấm "ゲストとして利用する"<br>2. Duyệt menu, thêm 1 món vào giỏ<br>3. Checkout → nhập `COMP-CROSS-001` → thanh toán qua elepay test card<br>4. Ghi lại Order ID<br>5. Navigate tới Link Email, nhập `qc_cross_full_20260605@test.com`<br>6. Nhập OTP → Set password `Cross@Full#2026` → Confirm<br>7. Mở Order History | 1. Mỗi bước thực hiện đúng theo happy path<br>2. Sau upgrade: Order History có đơn đã đặt trước đó<br>3. 7 menu items bị ẩn trước đây đã accessible<br>4. Không có lỗi nào trong toàn bộ flow | Email: `qc_cross_full_20260605@test.com`<br>Password: `Cross@Full#2026`<br>Test card: `4000000000000002` / `12/28` / `123` | Critical |
| GM_CROSS_TC_002 | Cross Flow | Guest Session Recovery | High | Check re-open app sau nhiều ngày — token vẫn valid → tái dùng guest session | Đã đăng nhập guest cách đây > 1 ngày, token chưa hết hạn, secure storage intact | 1. Ghi lại userId và email của guest session trước<br>2. Background app > 24 giờ (hoặc mock thời gian)<br>3. Mở lại app | 1. App không hiển thị màn Login<br>2. Vào thẳng app shell với cùng guest session<br>3. userId và email guest giống với session trước<br>4. Order history cũ vẫn truy cập được | Guest session token lifetime: theo config | High |
| GM_CROSS_TC_003 | Cross Flow | Toggle OFF giữa checkout | Critical | Check Admin tắt guestPaymentAllowed giữa lúc guest đang ở màn thanh toán | Guest đang ở màn chọn payment method sau khi validate Company ID thành công, Admin sắp tắt toggle | 1. Guest validate Company ID `COMP-CROSS-002` (đang ON) thành công<br>2. Guest đang nhìn màn chọn payment method<br>3. Admin (E03) vào Edit Company `COMP-CROSS-002`, tắt toggle, Save<br>4. Guest bấm "Pay" (elepay) | 1. API `/checkout` hoặc API xử lý payment trả về lỗi (403 hoặc business error)<br>2. App hiển thị thông báo lỗi rõ ràng (không silent fail)<br>3. Guest không bị charge tiền<br>4. Order không được tạo với status "paid" | Company: `COMP-CROSS-002` (toggle chuyển từ ON sang OFF trong lúc guest checkout) | Critical |
| GM_CROSS_TC_004 | Cross Flow | Email collision trong upgrade flow | Critical | Check email đã tồn tại (full account) → block, không merge data, order history guest vẫn là của guest | Đã đăng nhập guest với 2 orders, email `existing_account@test.com` đã là full account với 3 orders khác | 1. Navigate tới Link Email<br>2. Nhập `existing_account@test.com`<br>3. Bấm "送信" | 1. API trả lỗi email đã tồn tại<br>2. Error message: "このメールアドレスは既に使用されています..."<br>3. OTP không được gửi<br>4. Guest account không thay đổi<br>5. Orders của `existing_account@test.com` vẫn chỉ có 3 orders gốc (không thêm orders từ guest)<br>6. Guest vẫn có thể xem 2 orders trong session của mình | Email: `existing_account@test.com` (account đã có 3 orders)<br>Guest: có 2 orders | Critical |
| GM_CROSS_TC_005 | Cross Flow | Deeplink hidden route khi token expired | High | Check guest với token expired truy cập deeplink bị restrict | Token đã expire, app chưa tạo guest mới | 1. Force expire token<br>2. Mở deeplink `eskitchen://notifications` | 1. App detect token expired<br>2. Navigate về màn Login (không crash)<br>3. Không hiển thị bất kỳ data protected nào | Deeplink: `eskitchen://notifications` | High |
| GM_CROSS_TC_006 | Cross Flow | Company tắt guestPaymentAllowed giữa lúc guest đang xem menu | High | Check company tắt guestPaymentAllowed không ảnh hưởng đến việc duyệt menu | Guest đang duyệt menu của Company `COMP-CROSS-003`, Admin tắt guestPaymentAllowed | 1. Guest đang xem menu của `COMP-CROSS-003`<br>2. Admin tắt guestPaymentAllowed cho `COMP-CROSS-003`<br>3. Guest bấm thêm món vào giỏ | 1. Duyệt menu và thêm vào giỏ vẫn hoạt động bình thường (guestPaymentAllowed chỉ ảnh hưởng checkout)<br>2. Lỗi chỉ xuất hiện khi guest thực sự vào bước checkout + nhập Company ID | Company: `COMP-CROSS-003` | High |
| GM_CROSS_TC_007 | Cross Flow | Multiple guest sessions on same device | Medium | Check tạo nhiều guest session liên tiếp trên cùng 1 thiết bị không gây conflict | Thiết bị fresh install | 1. Bấm Guest Mode → guest 1 tạo, ghi userId1<br>2. Xóa app<br>3. Cài lại app → bấm Guest Mode → guest 2 tạo, ghi userId2<br>4. Xóa app lần 2<br>5. Cài lại app → bấm Guest Mode → guest 3, ghi userId3 | 1. Ba userId đều khác nhau<br>2. Ba email guest đều có format đúng (`guest_<8chars>@eskitchen.local`)<br>3. Mỗi session hoàn toàn độc lập | — | Medium |
| GM_CROSS_TC_008 | Cross Flow | Guest checkout sau khi upgrade (type = registered) | High | Check sau khi upgrade thành full account, checkout flow không còn yêu cầu nhập Company ID | Vừa upgrade thành công, account type = registered | 1. Thêm món vào giỏ<br>2. Bắt đầu checkout | 1. Checkout flow của registered user (không có step nhập Company ID)<br>2. Cash payment option xuất hiện (nếu company cho phép)<br>3. Hành xử như full user, không phải guest | — | High |
| GM_CROSS_TC_009 | Cross Flow | Out of scope — FCM notification không đăng ký cho guest | Medium | Verify guest KHÔNG nhận push notification (FCM token không được đăng ký) | Đã đăng nhập guest, app đang chạy | 1. Kiểm tra log/debug: có gọi FCM registration API không<br>2. Admin gửi thử broadcast notification | 1. FCM token registration không được gọi với guest token<br>2. Guest không nhận push notification<br>3. Không có lỗi về permission notification với guest | — | Medium |
| GM_CROSS_TC_010 | Cross Flow | Out of scope — Refund không áp dụng cho guest | Medium | Verify guest KHÔNG thể request refund | Đã đăng nhập guest, đã đặt và thanh toán 1 đơn | 1. Mở Order History<br>2. Chọn order đã thanh toán<br>3. Tìm nút Refund/返金 | 1. Không có nút Refund trong order detail của guest<br>2. Nếu cố gọi API refund → trả về lỗi unauthorized hoặc business error | — | Medium |
| GM_CROSS_TC_011 | Cross Flow | Out of scope — Guest không tham gia promotion | Low | Verify guest không thể dùng promotion/coupon code | Đã đăng nhập guest, có promotion code | 1. Thử nhập promotion code trong checkout (nếu UI có field này) | 1. Field promotion code không hiển thị với guest<br>2. Hoặc nếu có field, API từ chối với error rõ ràng | Promotion code: bất kỳ valid code | Low |
| GM_CROSS_TC_012 | Cross Flow | Security — Guest không truy cập data của user khác qua API | Critical | Check API endpoint order history filter đúng theo userId, không trả data của user khác | Guest A và Guest B đều đã đặt đơn | 1. Đăng nhập Guest A<br>2. Gọi `GET /orders` với accessToken của Guest A<br>3. Kiểm tra response | 1. Response chỉ chứa orders của Guest A<br>2. Không có order của Guest B hoặc bất kỳ user nào khác<br>3. API không chấp nhận userId override trong query params | Guest A token vs Guest B orders | Critical |
| GM_CROSS_TC_013 | Cross Flow | Security — Registered user không dùng được guest endpoint | High | Check `POST /auth/guest` không thể bị gọi bởi registered user để tạo duplicate guest | Đã đăng nhập với full account, có accessToken | 1. Gọi `POST /auth/guest` với header `Authorization: Bearer <registered_token>` | 1. API từ chối (400 hoặc 409)<br>2. Không tạo guest account mới gắn với registered user<br>3. Không có session conflict | Registered user token | High |
